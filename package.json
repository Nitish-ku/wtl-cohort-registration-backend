{
  "name": "wtl-cohort-registration-backend",
  "version": "1.0.0",
  "description": "Wired To Launch — standalone registration backend for the 72-Hour Founder Sprint cohort landing page (wtl-cohort-landing-v3). Registration only, no sprint-tool routes.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "firebase-admin": "^12.0.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
